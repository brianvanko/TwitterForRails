<h2>tweet index</h2>

<% @tweets.each do |tweet| %>
	<ul>
		<li><%= tweet.body %></li>
	</ul>
<% end %>

<%= simple_form_for @tweet do |f| %>
	<%= f.input :body %><br />
	<%= f.submit "Tweet", class: 'btn btn-primary' %>
<% end %>